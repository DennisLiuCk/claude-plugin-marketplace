# Claude Plugin Marketplace - 展示網站

這是 Claude Plugin Marketplace 繁體中文版的靜態展示網站，使用 GitHub Pages 託管。

## 🎨 設計理念

本網站採用「精緻的賽博工業主義」（Refined Cyber-Industrialism）設計風格：

- **配色方案**：深藍灰基底 + 青綠色強調 + 琥珀色標籤
- **字體選擇**：Outfit（標題）+ Space Mono（程式碼元素）+ Manrope（正文）
- **動畫效果**：交錯顯示、發光效果、平滑過渡、動態背景
- **視覺特色**：不對稱網格、微妙的網格紋理、創意動畫

設計遵循 `frontend-design` plugin 的指導原則，避免通用的 AI 美學，追求獨特且專業的視覺體驗。

## 📁 專案結構

```
docs/
├── index.html           # 主頁面
├── css/
│   └── style.css       # 完整的樣式系統
├── js/
│   ├── plugins-data.js # Plugin 資料（自動生成）
│   └── main.js         # 主要互動邏輯
└── README.md           # 本文件
```

## 🚀 功能特色

### 1. 插件展示
- 卡片式佈局展示所有 plugins
- 每個卡片包含：圖標、名稱、描述、版本、分類、來源標籤
- 懸停效果和動畫提升使用體驗

### 2. 篩選功能
- **分類篩選**：開發工具、生產力、安全、學習
- **來源篩選**：官方、社群（為未來擴展預留）
- 實時更新顯示結果

### 3. 搜尋功能
- 即時搜尋 plugin 名稱和描述
- 支援中英文搜尋
- 鍵盤快捷鍵：`Ctrl/Cmd + K` 聚焦搜尋框

### 4. 響應式設計
- 完美支援桌面、平板、手機
- 流暢的動畫和過渡效果
- 優化的觸控體驗

### 5. 效能優化
- 輕量級純 JavaScript（無框架依賴）
- 優化的 CSS 動畫
- 懶載入和交錯動畫

## 🔧 本地開發

### 預覽網站

直接在瀏覽器中打開 `docs/index.html` 文件，或使用本地伺服器：

```bash
# 使用 Python 啟動簡單的 HTTP 伺服器
cd docs
python3 -m http.server 8000

# 或使用 Node.js 的 http-server
npx http-server docs -p 8000
```

然後訪問 http://localhost:8000

### 更新 Plugin 資料

當 `marketplace.json` 或 plugin metadata 更新時，運行生成腳本：

```bash
python3 scripts/generate-plugins-data.py
```

這會自動更新 `docs/js/plugins-data.js` 文件。

## 📦 部署到 GitHub Pages

### 方法 1：手動部署

1. 在 GitHub repository 設定中啟用 GitHub Pages
2. 選擇 `main` 分支的 `/docs` 目錄作為來源
3. 儲存設定，等待部署完成

### 方法 2：自動部署（建議）

使用 GitHub Actions 自動部署（workflow 文件位於 `.github/workflows/`）。

每次推送到 `main` 分支時，會自動：
1. 生成最新的 plugin 資料
2. 部署到 GitHub Pages

## 🎯 未來擴展

### 支援社群 Plugins

當添加社群開發的 plugins 時：

1. 在 `marketplace.json` 中為 plugin 添加 `sourceType` 字段：
```json
{
  "name": "my-plugin",
  "sourceType": "community",
  ...
}
```

2. 更新 `scripts/generate-plugins-data.py` 中的 `get_source_type()` 函數
3. 重新生成資料：`python3 scripts/generate-plugins-data.py`

### 添加新的 Plugin 圖標

在 `scripts/generate-plugins-data.py` 的 `PLUGIN_ICONS` 字典中添加：

```python
PLUGIN_ICONS = {
    'my-new-plugin': '🎉',
    ...
}
```

### 添加詳細頁面

可以為每個 plugin 創建詳細頁面：
- 創建 `docs/plugins/` 目錄
- 為每個 plugin 生成 HTML 頁面
- 更新主頁面的連結

## 🛠 技術棧

- **HTML5**：語義化標記
- **CSS3**：
  - CSS Variables（設計系統）
  - Flexbox & Grid（佈局）
  - CSS Animations（動畫效果）
  - Media Queries（響應式設計）
- **JavaScript (ES6+)**：
  - Vanilla JS（無框架）
  - DOM Manipulation
  - Event Handling
  - Intersection Observer（滾動動畫）

## 📝 維護指南

### 修改樣式

所有樣式都在 `docs/css/style.css` 中。使用 CSS Variables 可以快速調整：

```css
:root {
    --color-accent-primary: #00d4aa;  /* 主強調色 */
    --font-primary: 'Outfit', sans-serif;  /* 主字體 */
    ...
}
```

### 修改互動邏輯

主要邏輯在 `docs/js/main.js` 中：
- `filterPlugins()` - 篩選邏輯
- `renderPlugins()` - 渲染邏輯
- `createPluginCard()` - 卡片生成

### 添加新功能

1. 在 `index.html` 中添加 HTML 結構
2. 在 `style.css` 中添加樣式
3. 在 `main.js` 中添加互動邏輯

## 🎨 設計資源

本網站的設計遵循以下原則（來自 `frontend-design` plugin）：

- ✅ 獨特的字體選擇（避免 Inter、Roboto、Arial）
- ✅ 創意的配色方案（避免紫色漸變等陳詞濫調）
- ✅ 有趣的動畫和互動效果
- ✅ 精心設計的排版和空間佈局
- ✅ 豐富的視覺細節（網格、噪聲、漸變）

## 📄 授權

本專案基於 Anthropic 官方 plugins，遵循各個 plugin 的原始授權。

## 🤝 貢獻

歡迎提交 Issue 和 Pull Request！

---

**維護者**：Dennis Liu
**版本**：1.1.0
**最後更新**：2025
